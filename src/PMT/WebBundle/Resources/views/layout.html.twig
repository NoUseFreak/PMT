<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>PMT</title>
    <meta name="description" content=""/>
    <meta name="author" content="Dries De Peuter"/>

    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <link rel="shortcut icon" href="{{ asset('favicon.ico') }}"/>

    <link href="{{ asset('bundles/pmtweb/css/bootstrap.min.css') }}" rel="stylesheet"/>
    <link href="{{ asset('bundles/pmtweb/vendor/select2/select2.css') }}" rel="stylesheet"/>
    <link href="{{ asset('bundles/pmtweb/css/select2-bootstrap.css') }}" rel="stylesheet"/>
    <link href="{{ asset('bundles/pmtweb/css/main.css') }}" rel="stylesheet"/>

    <script type="text/javascript" src="{{ asset('bundles/pmtweb/js/jquery.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/pmtweb/vendor/select2/select2.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/pmtweb/vendor/momentjs/moment.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/pmtweb/js/main.js') }}"></script>
    <script type="text/javascript">
        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', '{{ google_analytics_property_id }}']);
        _gaq.push(['_trackPageview']);
        (function() {
            var ga = document.createElement('script'); ga.type = 'text/javascript';
            ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' :
                    'http://www') + '.google-analytics.com/ga.js';
            var s = document.getElementsByTagName('script')[0];
            s.parentNode.insertBefore(ga, s);
        })();
    </script>

    {% block head_additions %}{% endblock %}
</head>

<body>

{% if is_granted('ROLE_USER') %}
    <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
            <div class="container-fluid">
                {{ render(controller('PMTWebBundle:Menu:primary')) }}
            </div>
        </div>
    </div>
{% endif %}

<div class="container-fluid">

    {% if is_granted('ROLE_USER') %}
        {% block content_header %}
            <div class="row-fluid">
                <div class="span6">
                    <h1>{% block content_title %}{% endblock %}</h1>
                </div>
                <div class="span6">
                    {% block content_actions %}
                        <ul class="actions nav nav-pills pull-right">
                            <li class="active">
                                <a href="{{ path('pmtweb_issue_form') }}"><i class="icon-edit icon-plus"></i> Create
                                    issue</a>
                            </li>
                        </ul>
                    {% endblock %}
                </div>
            </div>
        {% endblock %}
    {% endif %}

    {% for name, flashbag in app.session.flashbag %}
        {% for message in flashbag %}
        <div class="alert alert-{{ name }}">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            {{ message }}
        </div>
        {% endfor %}
    {% endfor %}

    {% block content %}
    {% endblock %}

</div>

<script src="{{ asset('bundles/pmtweb/js/bootstrap.min.js') }}"></script>
{% block body_scripts %}{% endblock %}
</body>
</html>
